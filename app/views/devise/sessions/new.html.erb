<%= stylesheet_link_tag 'devise_css/new'%>

<div class="row">
  <div class="col-md"></div>
  <div class="col-md-5" style="text-align: center;">
    <h1>Inicia Sesion</h1>
    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

      <div class="container" style="text-align: left;">

        <b><%= f.label :login%></b>
        <%= f.text_field :login, class: "form-control",placeholder: "Ingrese Usuario"%><br />

        <b><%= f.label :password %></b>
        <%= f.password_field :password, autocomplete: "current-password", class: "form-control",placeholder: "Ingrese Contraseña" %><br />

        <%= f.submit "Log in", class: "btn btn-dark btn-block" %>

        <% if devise_mapping.rememberable? -%>
          <%= f.check_box :remember_me, class: "form-check-label"%>
          <%= f.label :remember_me %>
        <% end -%>

        <%= link_to "Sign in with Google", user_google_oauth2_omniauth_authorize_path %>
      </div>

    <% end %>

    <div class="container" style="background-color:#f1f1f1">
      <br>
      <%= link_to "Regresar", :back, class: "btn btn-dark", id: "cancelbtn" %>
      <%= render "devise/shared/links" %>
    </div>

  </div>
  <div class="col-md"></div>
</div>