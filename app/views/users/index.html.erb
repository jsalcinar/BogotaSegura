<%= stylesheet_link_tag 'users/index' %>

<% @users = User.all %>

<h1 align="center">User List</h1>

<% @users.each do |user| %>
  <li>
    <%=link_to user.username, "users/"+user.username%> - <%= user.email %>
  </li>
  <% end %>